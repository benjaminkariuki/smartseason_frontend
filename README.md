## smartseason_frontend

Frontend for the SmartSeason application — built with React and Vite.

This repository contains the web UI used to manage fields, agents, and crop lifecycle tracking.

Development - Run `npm install` to install dependencies - Run `npm run dev` to start the dev server

License - See the project owner for license details.
